---
description: Update ~/.pi/agent/HANDOVER.md so future sessions have full context
argument-hint: "[anything new to add]"
---
Update the handover document at ~/.pi/agent/HANDOVER.md.

Steps:

1. Read the existing ~/.pi/agent/HANDOVER.md first.
2. Update it so a brand-new session in any project can pick up where we left off:
   - Refresh anything that changed this session (project status, config changes,
     technical state, decisions made).
   - Add anything new from $ARGUMENTS (if provided).
   - Keep it concise — cut stale detail, don't grow it endlessly.
3. Preserve the structure: setup/config notes, one part per active project
   (non-negotiable rules and critical structural rules stay verbatim),
   then "How Tim works".
4. Update the "Last updated" date at the bottom.
5. Show me a short summary of what you changed.
